<div class="page-header">
<h1></h1>
</div>

<div class='row'> 
	<div class="col-md-4"></div>
	<div class="col-md-4 well shadow-z-4">
	<%= form_for @subject do |f| %>
		<%= f.label :name %>
		<%= f.text_field  :name, class: 'form-control' %>
		<%= f.submit "Edit", class: "btn btn-primary" %>
	<% end %>
	</div>
	<div class="col-md-4"></div>

</div>